const User = require('./user.model');
const Job = require('./job.model');
const Resume = require('./resume.model');
const CoverLetter = require('./cover_letter.model');
const Application = require('./Application.model');
const JobMatch = require('./job_match.model');

module.exports = {
  User,
  Job,
  Resume,
  CoverLetter,
  Application,
  JobMatch
};
